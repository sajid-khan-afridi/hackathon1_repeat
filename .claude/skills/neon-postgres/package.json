{
  "name": "neon-postgres",
  "version": "1.0.0",
  "description": "Skill for managing Neon Serverless Postgres database operations",
  "main": "index.js",
  "type": "module",
  "dependencies": {
    "pg": "^8.11.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.2",
    "@types/uuid": "^9.0.1"
  },
  "scripts": {
    "migrate": "node -e \"import('./src/migration.js').then(m => m.runMigrations())\"",
    "test": "node --test"
  }
}