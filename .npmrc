# Disable telemetry
TELEMETRY_DISABLED=1

# Always install dependencies with exact versions
save-exact=true

# Use npm registry
registry=https://registry.npmjs.org/

# Optional: For private packages (not needed for this project)
# @yourorg:registry=https://npm.pkg.github.com/